PORT=4000
MONGO_URI=mongodb://localhost:27017/bidcraft
JWT_SECRET=your_jwt_secret_here
# Option 1: set FIREBASE_SERVICE_ACCOUNT to the JSON service account string (no newlines)
# FIREBASE_SERVICE_ACCOUNT='{"type": "...", "project_id":"...", ... }'
# Option 2: set GOOGLE_APPLICATION_CREDENTIALS to the path of the service account JSON file
# Place the downloaded JSON at the project root and set:
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
# Note: GOOGLE_CLIENT_ID is not required when frontend uses Firebase Auth to obtain ID tokens.

# Groq API settings (only API key required for SDK)
# GROQ_API_KEY=your_groq_api_key_here
# GROQ_MODEL=llama-3.3-70b-versatile